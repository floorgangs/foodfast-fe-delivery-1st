# API Configuration
# Choose one of these methods:

# Method 1: Custom URL (recommended for production)
# EXPO_PUBLIC_API_URL=https://your-api-domain.com

# Method 2: NGROK (easiest for testing across networks)
# Get ngrok URL from: ngrok http 5000
# EXPO_PUBLIC_NGROK_URL=https://your-ngrok-url.ngrok.io

# Method 3: LAN IP (local network only)
# Change this to your machine's IP: ipconfig | findstr IPv4
# EXPO_PUBLIC_LAN_IP=192.168.1.XX

# For development, update api.ts with one of above
